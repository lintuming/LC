/*
 * @lc app=leetcode id=79 lang=javascript
 *
 * [79] Word Search
 */

// @lc code=start
/**
 * @param {character[][]} board
 * @param {string} word
 * @return {boolean}
 */
var exist = function(board, word) {
  const length = word.length;
  const buffer = [];
  function backTrack(x, y, prevDirection) {
    for (let i = 0; i < board.length; i++) {}
  }
};
// @lc code=end
