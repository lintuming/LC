function solution(arr) {
  const result = [];
  const hashMap = {};
  if (arr.length < 3) {
    return result;
  }

  const target = 0;
  arr = arr.sort((a, b) => a - b);
  for (let i = 0; i < arr.length; i++) {}
}
